{
  "buildCommand": "npm run db:generate --workspace=server && npm run db:deploy --workspace=server && npm run build --workspace=server && npm run build --workspace=client",
  "outputDirectory": "client/dist",
"rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
